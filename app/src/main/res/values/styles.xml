<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="buttonStyle">@style/MyButton</item>
    </style>
    <style name="AppTheme2" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="buttonStyle">@style/MyButton</item>
    </style>

    <style name="CustomAppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowContentOverlay">@null</item>

        <item name="selectableItemBackground">?android:selectableItemBackground</item>
        <item name="selectableItemBackgroundBorderless">?android:selectableItemBackground</item>


    </style>
    <style name="btnSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/yelow</item>
        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/yelow</item>
        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/black</item>
    </style>
    <style name="MyButton" parent="Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Home map scene -->

    <style name="PrayButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:theme">@style/Theme.PrayButton</item>
    </style>

    <style name="Theme.PrayButton" parent="Widget.AppCompat.Button.Colored">
        <item name="colorAccent">@color/colorPrayButtonAccent</item>
        <item name="colorPrimary">@color/colorPrayButtonPrimary</item>
    </style>

    <style name="StopPrayButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:theme">@style/Theme.StopPrayButton</item>
    </style>

    <style name="Theme.StopPrayButton" parent="Widget.AppCompat.Button.Colored">
        <item name="colorAccent">@color/colorPrayButtonPrimary</item>
        <item name="colorPrimary">@color/colorPrayButtonAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="Editext_sign_up" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#000000</item>
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:padding">5dp</item>
        <item name="android:background">@drawable/bg_editext_form1</item>
        <item name="android:typeface">monospace</item>
    </style>
    <style name="tv_sign_up_result" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">3</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">20dp</item>
    </style>
</resources>
